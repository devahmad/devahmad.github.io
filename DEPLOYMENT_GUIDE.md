# 🚀 راهنمای دیپلوی روی GitHub Pages

## گام‌های دیپلوی سریع:

### 1. ایجاد ریپازیتوری جدید
1. به GitHub بروید و یه ریپازیتوری جدید بسازید
2. نام ریپازیتوری رو می‌تونید هر چیزی بذارید (مثلاً: `resume` یا `portfolio`)
3. ریپازیتوری رو عمومی (Public) انتخاب کنید

### 2. آپلود فایل‌ها
تمام فایل‌های زیر رو به ریپازیتوری جدیدتون آپلود کنید:
- `index.html` (فایل اصلی سایت)
- `styles.css` (استایل‌ها)
- `script.js` (جاواسکریپت)
- `404.html` (صفحه خطا)
- `_config.yml` (تنظیمات Jekyll)
- `README.md` (فایل راهنما)
- `.gitignore` (فایل ignore)

### 3. فعال‌سازی GitHub Pages
1. در ریپازیتوری، بروید به تب **Settings**
2. در منوی سمت چپ، بروید به **Pages**
3. در بخش **Source**، منوی کشویی رو باز کنید و **Deploy from a branch** رو انتخاب کنید
4. در بخش **Branch**، **main** یا **master** رو انتخاب کنید و روی **Save** کلیک کنید

### 4. مشاهده سایت
بعد از چند دقیقه، سایت شما در این آدرس در دسترس خواهد بود:
```
https://[your-username].github.io/[repository-name]/
```

مثال:
```
https://devahmad.github.io/resume/
```

## 🎯 نکات مهم:

### تنظیمات URL در _config.yml
حتماً در فایل `_config.yml`، مقادیر زیر رو به درستی تنظیم کنید:
```yaml
url: "https://[your-username].github.io"
baseurl: "/[repository-name]"
```

### سفارشی‌سازی اطلاعات
برای تغییر اطلاعات رزومه:
1. فایل `index.html` رو باز کنید
2. اطلاعات خودتون رو در بخش‌های مختلف جایگزین کنید
3. تغییرات رو ذخیره و commit کنید

### آپدیت خودکار
هر تغییری که در ریپازیتوری ایجاد کنید، به‌صورت خودکار روی سایت اعمال می‌شه.

## 📱 تست ریسپانسیو
برای تست نسخه موبایل:
1. در مرورگر Chrome، کلید F12 رو بزنید
2. روی آیکون موبایل کلیک کنید
3. اندازه صفحه رو تغییر بدید و طراحی رو بررسی کنید

## 🔧 عیب‌یابی

### اگر سایت لود نشد:
1. چک کنید که فایل `index.html` در ریشه ریپازیتوری باشه
2. مطمئن بشید که GitHub Pages در Settings > Pages فعال شده
3. چند دقیقه صبر کنید (ممکنه تا 10 دقیقه طول بکشه)

### اگر استایل‌ها لود نشدن:
1. مطمئن بشید که فایل `styles.css` درست آپلود شده
2. در فایل `index.html`، لینک CSS به درستی باشه

## 🌟 ویژگی‌های خاص این رزومه:
- ✅ دو زبانه (فارسی/انگلیسی) با دکمه سوییچ
- ✅ طراحی مدرن و یونیک
- ✅ کاملاً ریسپانسیو
- ✅ انیمیشن‌های حرفه‌ای
- ✅ قابلیت چاپ
- ✅ صفحه 404 سفارشی
- ✅ بهینه برای SEO
- ✅ سریع و سبک

## 📞 پشتیبانی
اگر مشکلی داشتید:
1. ایمیل: Job_goods@yahoo.com
2. تلگرام: @DEVAHMAD

---
**موفق باشید! 🎉**